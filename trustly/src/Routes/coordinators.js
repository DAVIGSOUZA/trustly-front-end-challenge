export const goToHomePage = (history) => {
    history.push('/')
}

export const goToCheckoutPage = (history) => {
    history.push('/checkout')
}

export const goToConfirmationPage = (history) => {
    history.push('/confirmation')
}